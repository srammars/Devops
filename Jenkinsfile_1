pipeline {
    agent any

    environment {
        MVN_HOME = tool 'maven-3.5.2'
    }

    stages {
        stage('Clone repo') {
            steps {
                checkout scm
            }
        }

        stage('Build and Test') {
            steps {
                dir('demo') {
                    script {
                        sh "${MVN_HOME}/bin/mvn -B clean test"
                    }
                }
            }
        }

        stage('Modify Test to Fail') {
            steps {
                script {
                    // Modify DummyTest.java to intentionally fail a test
                    sh 'echo "    assertEquals(4, add(2, 2)); // Intentional failure" >> demo/src/test/java/DummyTest.java'
                }
            }
        }

        stage('Build with Failing Test') {
            steps {
                dir('demo') {
                    script {
                        sh "${MVN_HOME}/bin/mvn -B clean test"
                    }
                }
            }
        }

        stage('Ignore Test') {
            steps {
                dir('demo') {
                    script {
                        sh "${MVN_HOME}/bin/mvn -B clean test -Dmaven.test.failure.ignore=true"
                    }
                }
            }
        }

        stage('Publish Test Report') {
            steps {
                junit 'demo/target/surefire-reports/*.xml'
            }
        }
    }

    post {
        always {
            echo 'This will always run'
        }
        success {
            echo 'This will run only if the build is successful'
        }
        failure {
            echo 'This will run only if the build fails'
        }
    }
}
